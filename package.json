{
  "name": "dreamcatcher",
  "version": "2.0.0",
  "private": true,
  "description": "DreamCatcher — zero-token job radar: scrapes career sites on a schedule, rule-filters postings, optionally lets Claude score new ones.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "scan": "node scraper/index.mjs",
    "scan:rules-only": "node scraper/index.mjs --no-score"
  }
}
